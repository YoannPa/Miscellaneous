# To install Python3 packages from the generated list run the following command in a terminal:
sudo python3 -m pip install -r /home/yoann/List_Installed_Python_Packages.txt

#In case you experience errors for specific packages, just remove them from the list.
