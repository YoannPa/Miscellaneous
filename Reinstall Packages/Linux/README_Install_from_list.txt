# To install packages from the generated list run the following command in a terminal:
cut -f1 List_Installed_Packages | xargs sudo apt install -y

#In case you experience errors for specific packages, just remove them from the list.
